import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.openforge.rockthesteps',
    appName: 'rock-the-steps',
    webDir: '../../dist/apps/rock-the-steps',
    bundledWebRuntime: false,
};

export default config;
